name: Deploy to Google Cloud Run

on:
  push:
    branches:
      - main # Trigger on push to main branch

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: asia-east2
  REPO_NAME: eightbitstore-repo
  IMAGE_NAME: eightbitstore-api
  SERVICE_NAME: eightbitstore-api

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write' # Required for Workload Identity Federation

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: '${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}'
          service_account: '${{ secrets.GCP_SERVICE_ACCOUNT }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker to use Artifact Registry
        run: gcloud auth configure-docker $REGION-docker.pkg.dev --quiet

      - name: Build Docker Image
        working-directory: ./8bitstore-be
        run: |
          docker build -t $REGION-docker.pkg.dev/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:${{ github.sha }} .

      - name: Push Docker Image
        run: |
          docker push $REGION-docker.pkg.dev/$PROJECT_ID/$REPO_NAME/$IMAGE_NAME:${{ github.sha }}

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE_NAME }}
          region: ${{ env.REGION }}
          image: ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          env_vars: |
            ASPNETCORE_ENVIRONMENT=Production
            Vnpay__TmnCode=Y3E1QYB5
            Vnpay__BaseUrl=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
            Vnpay__PaymentBackReturnUrl=https://8bitstore-brown.vercel.app/payment-processing
            EmailSetting__SmtpHost=smtp.gmail.com
            EmailSetting__SmtpPort=587
            EmailSetting__SmtpUser=minhtuan034015@gmail.com
            EmailSetting__FromEmail=minhtuan034015@gmail.com
          secrets: |
            ConnectionStrings__WebApiDatabase=store-db-connection:latest
            Redis__ConnectionString=redis-connection:latest
            Vnpay__HashSecret=vnpay-hashsecret:latest
            EmailSetting__SmtpPass=smtp-pass:latest
          flags: '--allow-unauthenticated --port=8080'
